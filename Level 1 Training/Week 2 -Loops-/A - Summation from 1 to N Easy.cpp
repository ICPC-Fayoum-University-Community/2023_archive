#include <iostream>
using namespace std;

/*
 * as simple as iterating from 1 to N and calculating the summation
 */

int main() {
    int n;
    cin >> n;
    
    long long sum = 0;
    
    for (int i = 1; i <= n; ++i) {
        csum += i;
    }
    
    cout << sum;

    return 0;
}
